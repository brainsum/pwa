/**
 * @file
 * Classy Subtheme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.classySubtheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
